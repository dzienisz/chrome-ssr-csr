{
  "name": "ssr-csr-detector-extension",
  "version": "3.5.0",
  "description": "Chrome extension to detect SSR vs CSR rendering",
  "scripts": {
    "build": "node scripts/build-bundle.js",
    "build:watch": "nodemon --watch src --exec 'npm run build'",
    "lint": "echo 'Linting not configured yet'",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^27.4.0",
    "nodemon": "^3.0.2",
    "vitest": "^4.0.18"
  },
  "keywords": [
    "chrome-extension",
    "ssr",
    "csr",
    "performance",
    "web-vitals"
  ],
  "author": "Kamil Dzieniszewski",
  "license": "MIT"
}
